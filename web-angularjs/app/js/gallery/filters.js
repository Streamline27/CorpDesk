/**
 * Created by Aleksej_home on 2016.01.04..
 */
/*
var galleryFilters = angular.module('galleryFilters', []);

galleryFilters.filter('between', function() {
    return function(input, total) {

        start = parseInt(total.start);
        end = parseInt(total.end);

        for (var i=start; i<end; i++) {
            input.push(i);
        }

        return input;
    };
});*/